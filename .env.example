# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-email-password
SMTP_FROM=no-reply@twojadomena.pl

# Security & JWT
# Generate strong key, e.g. by running: openssl rand -hex 32
JWT_SECRET=change-me-in-production-12345

# Logic Settings
ESCALATION_DELAY_SECONDS=300